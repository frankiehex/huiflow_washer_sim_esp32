# ESPHome build artifacts
.esphome/
**/build/
**/.pioenvs/
**/.piolibdeps/

# Python
__pycache__/
*.pyc

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Secrets：真實密碼絕不 commit，只提交 secrets.example.yaml template
secrets.yaml
.env
.env.local

# Binary artifacts 跟主板慣例一致提交（方便用戶 git pull 直接 esptool 燒錄）
# 每次 build 覆寫版本號對應的 bin，git diff 只會看到 binary summary 不污染
